# API Configuration
# APIのオリジン（スキーム + ホスト + ポートのみ。パスは含めない）
# 開発時: Vite プロキシ経由のため空でよい（vite.config.ts の proxy 設定が転送先を持つ）
# 本番時: 別オリジンになるため絶対 URL を指定する（例: https://api.shifty.example.com）
VITE_API_BASE_URL=

# デバッグモード（開発環境でのみ有効にすることを推奨）
VITE_DEBUG=false
